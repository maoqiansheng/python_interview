

数据库的优化：

1、增加索引，优化SQL语句，分析查询慢的原因；

2、设计表的时候严格按照数据库的设计范式设计
    第一范式：强调列的原子性，即列不可再分
    第二范式：表必须有主键，非主键字段必须完全依赖主键
    第三范式：非主键字段必须直接依赖于主键，不可以间接依赖

3、使用缓存，把经常使用到又不会经常变化的数据存到缓存中去

4、优化硬件，使用SSD,使用磁盘队列技术(RAID0,RAID1,RDID5)等

5、主从分离读写；采用主从复制把数据库的读操作和写入操作分离开来；


7、进行架构级别的缓存，静态化和分布式；


